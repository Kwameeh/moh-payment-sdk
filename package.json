{
  "name": "moh-payment-sdk",
  "version": "1.0.0",
  "private": true,
  "description": "MOH Payment Gateway SDK for frontend applications",
  "scripts": {
    "dev": "pnpm --filter payment-sdk dev",
    "build": "pnpm --filter payment-sdk build",
    "test": "pnpm --filter payment-sdk test",
    "lint": "pnpm --filter payment-sdk lint"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
